{
  "name": "swasthya-hospital-root",
  "private": true,
  "description": "Root delegator for Railway deployment",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start"
  }
}
